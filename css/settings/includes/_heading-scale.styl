heading-scale( ratio = 1.2, top = 2, bottom = -0.125, shift = 0 )

  headings()
    power = 6 - (i - shift)
    size = (ratio**power)em
    font-size size
    margin (ratio/size)*top 0 (ratio/size)*bottom !important
    line-height 1.3

  for i in (1..6)
    h{i}
      headings()